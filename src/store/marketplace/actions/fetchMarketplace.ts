import type { MarketplaceStoreProps } from "./utility/types";

export const fetchMarketplace = async (
  set: (state: Partial<MarketplaceStoreProps>) => void,
  get: () => MarketplaceStoreProps,
) => {
 
}